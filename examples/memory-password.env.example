# Personal full-access Research Memory connection. Keep the real file ignored.
# Quote the password if it contains shell-special characters; never commit it.
MEMORY_HOST=147.47.39.138
MEMORY_USER=memory-rpc
MEMORY_PASSWORD=
# Leave empty to let an agent list projects and ask which one to use.
MEMORY_PROJECT=
